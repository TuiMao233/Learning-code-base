## 项目中利用axios封装ajax函数

~~~javascript
import axios from 'axios'
// ajax请求函数模块
// 返回值:promise对象(异步返回的数据是: response.data)
export default function (url = '', data = {}, type = 'GET') {
    return new Promise(function (resolve, reject) {
        // 执行异步ajax请求
        let promise
        if (type === 'GET') { // 如果type是get方法
          let dataStr = '' //
          Object.keys(data).forEach(key => {
            dataStr += key + '=' + data[key] + '&'
          })
          if (dataStr !== '') {
            dataStr = dataStr.substring(0, dataStr.lastIndexOf('&'))
            url += '?' + dataStr
          }
          // 发送get请求
          promise = axios.get(url)
        } else {
          // 发送post请求
          promise = axios.post(url, data)
        }
        promise.then(function (response) { // 请求成功
          resolve(response.data)
        }).catch(function (error) { // 请求失败
          reject(error)
        })
    })
}
~~~



## 利用ajax函数封装接口函数

利用ajax函数封装接口函数



## 利用git对项目进行版本控制 并上传仓库

~~~
基本用户设置
git config --global user.name "tuimao233"
git config --global user.email "951416545@qq.com"
vue项目目录运行npm代码
git init // 当前目录下创建本地仓库
git add * // 发布文件到暂存区
git commit -m "init [gshop]"	// 从暂存区--本地分支

--------github创建项目-------
-> 指定仓库名称
-> 指定说明 
-> 点击创建

git remote add origin https://github.com/TuiMao233/gshop.git
git push -u origin master
~~~

